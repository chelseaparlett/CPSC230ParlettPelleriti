#Dictionaries (How are they indexed? How do we create them?
# How do we add things to them? replace things? grab values?
# keys?)
#Functions (how do we define one? Why do we use them?
# what are default arguments and how do you define them?
# how to call a function.)
#File (how to open, close, and what mode to use?)

#OLD STUFF----------------------------
#For/while
#Strings, lists...
charDict = {"player1": {},
 "player2": {}}

choice = input()
 mychar = charDict[choice]

[random.randint(1,20) for i in range(0,5)]

"DD" : {"Age" : 14,
 ... ,
 "s" : {"name":"Flipkick", "range": [0,10], "cool": 4}}
 list(dict.keys())
 opponent = charDict["player2"]
def attack(special = False, d = chardict)
def listCheck():
    if st in li:
        return True
    else:
        return False
listCheck(li = [1,3,4,5])


while noOnesDied:
    playerturn(P1,CPU, True)
    if not checkhealth():
        break
    playerturn(CPA,P1, True)
    noOnesDied = checkhealth()
